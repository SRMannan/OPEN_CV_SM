import cv2
import numpy as np

image = cv2.imread("Stevie.png")

rows,cols = image.shape[:2]

matrix = cv2.getRotationMatrix2D((rows/2 , cols/2) , 360 , 1)
new_image = cv2.warpAffine(image, matrix ,(cols,rows))

cv2.imshow('output' , new_image)

cv2.waitKey(0)
cv2.destroyAllWindows()
